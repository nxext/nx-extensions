import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: '<%= appId %>',
  appName: '<%= appName %>',
  webDir: '<%= webDir %>',
  server: {
    androidScheme: 'https'
  }
}

export default config;
