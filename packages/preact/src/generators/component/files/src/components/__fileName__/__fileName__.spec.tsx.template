import {h} from 'preact'
import {render} from '@testing-library/preact'

import <%= className %> from './<%= className %>'


it('it works', async () => {
  const { getByText } = render(h(<%= className %>, null))

  expect(getByText('Hello component!'));
})
