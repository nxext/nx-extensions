import { h } from '@stencil/core';

export default {
  title: '<%= className %>',
  args: {
    first: 'John',
    middle: 'S',
    last: 'Doe'
  }
};

export const Primary = (args) => {
  return (
    <<%= componentFileName %> {...args}></<%= componentFileName %>>
  )
};
