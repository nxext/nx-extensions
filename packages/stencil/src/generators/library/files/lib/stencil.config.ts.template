import { Config } from '@stencil/core';

export const config: Config = {
  namespace: '<%= name %>',
  taskQueue: 'async',
  sourceMap: true,

  extras: {
    experimentalImportInjection: true,
  }
};
