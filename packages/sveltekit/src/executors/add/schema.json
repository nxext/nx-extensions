{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Add executor",
  "description": "",
  "type": "object",
  "properties": {
    "package": {
      "description": "Package you want to install",
      "type": "string",
      "enum": ["tailwindcss", "mdsvex", "graphql", "postcss", "bulma", "windicss", "firebase-hosting"]
    }
  },
  "required": [
    "package"
  ]
}
