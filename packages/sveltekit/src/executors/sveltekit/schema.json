{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Dev executor",
  "description": "",
  "type": "object",
  "properties": {
    "command": {
      "type": "string",
      "description": "Sveltekit command to run"
    }
  },
  "required": [
    "command"
  ]
}
