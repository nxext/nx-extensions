import <%= fileName %> from './+page.svelte'
import { render } from '@testing-library/svelte'

it('it works', async () => {
  const { getByText } = render(<%= fileName %>)

  expect(getByText('Hello component!'));
})
