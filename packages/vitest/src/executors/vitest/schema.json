{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Vitest executor",
  "description": "",
  "type": "object",
  "properties": {
    "command": {
      "type": "string",
      "description": "Sveltekit command to run"
    }
  },
  "required": [
    "command"
  ]
}
