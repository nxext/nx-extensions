{
  "title": "Nx Plugin Playground Target",
  "description": "Runs the e2e tests with dependencies",
  "type": "object",
  "cli": "nx",
  "properties": {
    "targets": {
      "type": "array",
      "description": "the target Nx Plugin project and build"
    },
    "jestConfig": {
      "type": "string",
      "description": "Jest config file"
    },
    "tsSpecConfig": {
      "type": "string",
      "description": "Spec tsconfig file",
      "x-deprecated": true
    }
  },
  "required": ["targets", "jestConfig"]
}
